# 第 5 部分：什么是 MCP，为什么你应该关注？

---

## 首先，快速回顾

- **第 3 部分：** 我们了解到工具让模型能够**执行操作**。
- **第 4 部分：** 我们看到 RAG 帮助模型在回答前**找到相关信息**。

这些都是**外部支持**——它们帮助模型更智能地行动，但协调工作仍然在模型之外进行。

但如果你能以一种清晰、结构化的格式传递**模型所需的所有上下文**——工具、检索到的数据、记忆、指令——会怎么样？

这就是**模型上下文协议（MCP）**试图解决的问题。

---

## 那么，MCP 是什么？
<img width="737" height="452" alt="image" src="https://github.com/user-attachments/assets/2e17fcb6-ab35-4a4c-88b4-c74db165e4b8" />


从本质上讲，**模型上下文协议（MCP）**是一种标准化的方法，用来向 LLM 提供推理与响应所需的一切上下文。

可以把它想象成打包：

- 你希望模型完成的任务
- 它可以使用的工具/API
- 它可能需要的文档或记忆
- 对话中的先前消息

……然后把这些内容一次性、成体系地交付给模型。

它**不是**工具、库或产品。
它是一种**协议**——模型与外部世界之间的通信结构。

如果你来自技术领域，类似的例子有：**HTTP**、**TCP/IP** 或 **SMTP**。
如果你不是技术出身，只需记住：技术人员偏爱标准化，因为它能让东西更容易复用、组合与落地。

---

## 这为什么重要？

假设你正在构建一个智能体。
现在，你可能需要同时处理：

- 发送提示
- 传递检索到的文档
- 注册工具
- 管理状态
- 跟踪之前发生的事情

MCP 的主张是：
> “让我们标准化如何将所有这些内容提供给模型，这样我们就不必为每个用例重新发明轮子。”

对于**企业**来说，这非常重要。
随着智能体变得越来越复杂，协调**工具**、**RAG**、**记忆**和**输出**变得混乱。

MCP 使这种编排**可组合**、**模块化**，并更容易插入其他系统。

如果你曾经使用过 API，可以把 MCP 理解成一种**定义良好的请求载荷（request schema）**。
与其把所有内容塞进一长串提示词、再“祈祷模型能懂”，不如把上下文组织成**结构化**的形式，提供**清晰的背景、可用选项与可追溯依据**。

---

## 为什么 MCP 会迅速流行？

鉴于 MCP 只是一个协议，你可能会问：
> 它有什么优势，为什么每个人都开始使用它？

以下是原因：

1. **AI 原生** — MCP 是为 AI 智能体构建的。它为当今智能体使用的一切提供了空间：工具、提示、记忆、文档等等。
2. **完善的文档和示例** — Anthropic（MCP 的创建者）不仅发布了规范，还发布了客户端、SDK、测试工具和现实世界的演示。
3. **网络效应** — 2024 年 11 月低调发布时，多数人并未在意……但到了 2025 年迅速爆发；工具厂商、初创公司，甚至 OpenAI 都开始支持它。

---

## 常见误解

- **MCP 不是新的 API 或产品** — 它只是一种模式，一种清晰地组织发送给模型内容的方式。
- **它不会让模型更智能** — 它只是为模型提供更好、更结构化的上下文。
- **它不仅适用于智能体** — 即使是简单的助手也能从更好的上下文管理中受益。

---

## 那么……你应该关注吗？

如果你正在构建玩具提示或快速演示——可能不需要（目前）。

但如果你正在从事：

- 企业级智能体
- 多工具工作流
- 需要访问**记忆 + RAG + 规划**的 LLM
- **上下文管理**是瓶颈的系统

……那么**是的**，你应该关注。MCP 的核心在于：更好地把“不断演变、可结构化的上下文”传给模型。

但请记住：MCP 只是一个协议。
像所有标准一样，只有在广泛采用的情况下它才有效。
如果在 MCP 成为“智能体的 HTTP”之前出现了更好的东西，生态系统可能会再次转变。

---

## 延伸阅读与资源

- 我们写了一篇**[完整的深入文章](https://thenuancedperspective.substack.com/p/mcp-overhyped-misunderstood-and-actually)**，介绍了 MCP，包括客户端、服务器和现实世界的用例（由 OpenAI 的 Kiriti Badam 撰写）。
- 我们还举办了一次**免费直播**——你可以在这里观看[录播](https://maven.com/p/82345a)。

---

在下一部分中，我们将学习 Agentic 系统的**规划**组件及其重要性。

附言：我们还开设了一门广受欢迎的课程，专门讲如何在快速变化的环境中，以**问题优先**的方法真正落地 AI 系统，面向在现实约束下工作的产品经理、管理者、工程师与决策者等。校友来自 Google、Meta、Apple、Netflix、AWS、Spotify、Snapchat、Deloitte 等公司。我们的下一期课程即将开始；早鸟优惠已上线：使用代码 `GITHUB` 可享受 300 美元折扣（仅适用于 2025 年 8 月），请[在此注册](https://maven.com/aishwarya-kiriti/genai-system-design)！